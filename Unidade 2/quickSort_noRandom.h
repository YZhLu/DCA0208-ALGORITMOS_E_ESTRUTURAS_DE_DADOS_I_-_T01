#ifndef QUICKSORT_NORANDOM_DOT_H    /* This is an "include guard" */
#define QUICKSORT_NORANDOM_DOT_H    /* prevents the file from being included twice. */
                     /* Including a header file twice causes all kinds */
                     /* of interesting problems.*/

/**
 * This is a function declaration.
 * It tells the compiler that the function exists somewhere.
 */
void  quickSort_noRandom(int arr[], int p, int q);

#endif /* QUICKSORT_NORANDOM_DOT_H */